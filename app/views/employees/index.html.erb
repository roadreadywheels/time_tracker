<div class="header">
	<% @employees.each do |employee| %>
  	<%= link_to employee.name, employee_path(employee) %>
	<% end %>
</div>

<%= form_for Employee.new do |f| %>
	<%= f.text_field :name %>
	<%= f.submit %>
<% end %>